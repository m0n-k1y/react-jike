//统一中转导出  工具函数
//import {request} from "@/request";
import { request } from "./request";
import { getToken,setToken,removeToken } from "./token";

export{ 
  request,
  getToken,
  setToken,
  removeToken
}